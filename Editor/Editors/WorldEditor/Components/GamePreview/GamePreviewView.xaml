<UserControl x:Class="Editor.Editors.WorldEditor.Components.GamePreview.GamePreviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="600">

    <UserControl.Resources>
        <Style x:Key="ViewTabStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#808080"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Border" 
                                Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}"
                                BorderThickness="0,0,0,2"
                                BorderBrush="Transparent">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#3FA9F5"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#C5C5C5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ToolButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#808080"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="24"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#3C3C3C"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="#252526" CornerRadius="4" Margin="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="24"/>
            </Grid.RowDefinitions>

            <!-- Header with Tabs -->
            <Border Grid.Row="0" Background="#2D2D30" CornerRadius="4,4,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- View Tabs -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <RadioButton Content="Scene" IsChecked="True" Style="{StaticResource ViewTabStyle}" GroupName="ViewTabs"/>
                        <RadioButton Content="Game" Style="{StaticResource ViewTabStyle}" GroupName="ViewTabs"/>
                    </StackPanel>

                    <!-- Toolbar -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="8,0">
                        <Button Content="&#xE759;" Style="{StaticResource ToolButtonStyle}" ToolTip="Move Tool"/>
                        <Button Content="&#xE7AD;" Style="{StaticResource ToolButtonStyle}" ToolTip="Rotate Tool"/>
                        <Button Content="&#xE740;" Style="{StaticResource ToolButtonStyle}" ToolTip="Scale Tool"/>
                        <Rectangle Width="1" Fill="#404040" Margin="6,6"/>
                        <Button Content="&#xE8B3;" Style="{StaticResource ToolButtonStyle}" ToolTip="Grid"/>
                        <Button Content="&#xE81E;" Style="{StaticResource ToolButtonStyle}" ToolTip="Snap"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Preview Area -->
            <Border Grid.Row="1" Background="#1A1A1A">
                <Grid>
                    <!-- Placeholder Grid Pattern -->
                    <Canvas ClipToBounds="True">
                        <Canvas.Background>
                            <DrawingBrush TileMode="Tile" Viewport="0,0,40,40" ViewportUnits="Absolute">
                                <DrawingBrush.Drawing>
                                    <GeometryDrawing>
                                        <GeometryDrawing.Geometry>
                                            <GeometryGroup>
                                                <LineGeometry StartPoint="0,0" EndPoint="40,0"/>
                                                <LineGeometry StartPoint="0,0" EndPoint="0,40"/>
                                            </GeometryGroup>
                                        </GeometryDrawing.Geometry>
                                        <GeometryDrawing.Pen>
                                            <Pen Brush="#2A2A2A" Thickness="1"/>
                                        </GeometryDrawing.Pen>
                                    </GeometryDrawing>
                                </DrawingBrush.Drawing>
                            </DrawingBrush>
                        </Canvas.Background>
                    </Canvas>

                    <!-- Center Info -->
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="&#xE714;" FontFamily="Segoe MDL2 Assets" FontSize="48" 
                                   Foreground="#404040" HorizontalAlignment="Center"/>
                        <TextBlock Text="Scene View" Foreground="#505050" FontSize="14" 
                                   HorizontalAlignment="Center" Margin="0,8,0,0"/>
                        <TextBlock Text="Game Preview will render here" Foreground="#404040" FontSize="11" 
                                   HorizontalAlignment="Center" Margin="0,4,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Status Bar -->
            <Border Grid.Row="2" Background="#2D2D30" CornerRadius="0,0,4,4">
                <Grid Margin="8,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="FPS: 60 | Draw Calls: 0 | Vertices: 0" 
                               Foreground="#606060" FontSize="10" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1" Text="1920 x 1080" 
                               Foreground="#606060" FontSize="10" VerticalAlignment="Center"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
