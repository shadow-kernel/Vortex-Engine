<UserControl x:Class="Editor.Editors.WorldEditor.Components.HistoryLog.HistoryLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Editor.Editors.WorldEditor.Components.HistoryLog"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="280">

    <UserControl.DataContext>
        <local:HistoryLogViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <!-- List Item Style -->
        <Style x:Key="HistoryItemStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#E9E9ED"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}" CornerRadius="3">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#2C2C32"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="IsSelected" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Bd" Property="Background" Value="#2A2D2E"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="#212124">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="28"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#1A1A1C" Padding="8,4">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="History" Foreground="#E9E9ED" 
                               FontWeight="SemiBold" VerticalAlignment="Center"/>
                    
                    <!-- Undo Button -->
                    <Button Grid.Column="1" Content="&#xE7A7;" FontFamily="Segoe MDL2 Assets"
                            Width="22" Height="22" Margin="4,0,0,0"
                            Background="Transparent" Foreground="#9A9AA1" BorderThickness="0"
                            ToolTip="Undo" Click="Undo_Click" Cursor="Hand"/>
                    
                    <!-- Redo Button -->
                    <Button Grid.Column="2" Content="&#xE7A6;" FontFamily="Segoe MDL2 Assets"
                            Width="22" Height="22" Margin="4,0,0,0"
                            Background="Transparent" Foreground="#9A9AA1" BorderThickness="0"
                            ToolTip="Redo" Click="Redo_Click" Cursor="Hand"/>
                </Grid>
            </Border>

            <!-- Current State Indicator -->
            <Border Grid.Row="1" Background="#2D2D2D" Padding="8,6">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE73E;" FontFamily="Segoe MDL2 Assets" FontSize="12" 
                               Foreground="#4EC9B0" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBlock Text="Current State" Foreground="#4EC9B0" FontWeight="SemiBold"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- History List -->
            <ListBox Grid.Row="2" Background="Transparent" BorderThickness="0"
                     ItemsSource="{Binding HistoryItems}"
                     SelectedItem="{Binding SelectedItem}"
                     ItemContainerStyle="{StaticResource HistoryItemStyle}"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     MouseDoubleClick="HistoryList_MouseDoubleClick">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Icon based on type -->
                            <TextBlock Grid.Column="0" Text="{Binding Icon}" 
                                       FontFamily="Segoe MDL2 Assets" FontSize="12"
                                       Foreground="{Binding IconColor}" 
                                       VerticalAlignment="Center" Margin="0,0,8,0"/>
                            
                            <!-- Command Name -->
                            <TextBlock Grid.Column="1" Text="{Binding Name}" 
                                       Foreground="{Binding TextColor}"
                                       VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                            
                            <!-- Index -->
                            <TextBlock Grid.Column="2" Text="{Binding IndexDisplay}" 
                                       Foreground="#73737A" FontSize="10"
                                       VerticalAlignment="Center" Margin="8,0,0,0"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Border>
</UserControl>
